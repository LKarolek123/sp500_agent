#!/usr/bin/env python3
"""Entry point for sp500_agent pipeline (placeholder)."""
import sys
from pathlib import Path
sys.path.append(str(Path(__file__).parent / "src"))

if __name__ == "__main__":
    print("sp500_agent: pipeline entry (placeholders created).")
